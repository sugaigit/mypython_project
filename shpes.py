shapes = [trl,sql,crl]
for a_shape in shapes:
    if isinstance(a_shpe,triangle):
        a_shape.draw_triangle()
    if isinstance(a_shpe,Square):
        a_shpe.draw_square()
    if isinstance(a_shape,Circle):
        a_shape.draw_cicle()


shapes = [trl,swl,crl]
for a_shape in shapes:
    a_shepe.draw()
